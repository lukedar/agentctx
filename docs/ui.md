# Runner UI

The runner UI is local-first and optional.

Planned:
- SSE event stream
- append-only event store
- pure reducers to derive UI model + metrics
- dashboard showing pipeline steps, decisions, reviews, scores, token-usage status, timings, and security redactions
